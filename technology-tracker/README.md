# Трекер технологий (Miku)

### Как запустить:
1. `npm install` — установка всех зависимостей (включая MUI).
2. `npm start` (или `npm run dev`) — запуск проекта.

### Использованные технологии:
- React + React Router
- Material-UI (Темизация, Уведомления)
- LocalStorage для сохранения прогресса